<%= form_for(@user) do |f| %>
    <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <b>Gruppe:</b>
      <br/>
      <%= f.select :role,
                   ["User", "Administrator"], {}, {"size" => 2} %>
    </div>

    <div class="field">
      <b>Name:</b>
      <br/>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <b>Passwort</b>
      <br/>
      <%= f.password_field :password %>
    </div>
    <div class="field">
      <b>Passwort bestätigen:</b>
      <br/>
      <%= f.password_field :password_confirmation %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
